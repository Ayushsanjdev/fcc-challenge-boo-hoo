function booWho(bool) { //creating function first
	if (typeof(bool) === "boolean") { //using if statements to check 
		return true; //if bool's type is any boolean then return
	} //else
	return false; //false
}
